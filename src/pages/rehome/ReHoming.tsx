import { ReactElement } from 'react';
import RehomingTemplate from 'src/components/rehoming/ReHomingTemplate';

const ReHoming = (): ReactElement => {
  return <RehomingTemplate />;
};

export default ReHoming;
